language: objective-c
osx_image: xcode11.4
before_install:
- gem install bundler
- gem update bundler
- gem install fastlane
install: true
script:
- fastlane scan -p "ActiveLabel.xcodeproj" -s "ActiveLabel"
